describe('matching Title', function () {
    it('getting and matching title', function () {
        browser.get('http://www.way2automation.com/angularjs-protractor/registeration/#/login');
        // title=browser.getTitle();
        // title.then(function (text) {
        //     console.log(text);
            // expect(text).toBe('Protractor practice website - Registration');
            // expect(text).toEqual('Protractor practice website - Registration');
           // expect(text).toMatch('Protractor');
        }) ;
        // console.log();
    // });
});